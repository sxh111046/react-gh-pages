import { MenuItem } from "./MenuItem";

export interface MenuItems {
    items: MenuItem[];
}

export const PersonActions = ["Ancestors Path", "Decendents Path", "Root Tree", "Collapse Main Branches", "Expand All"];